## init

``` shell script
    git clone https://github.com/EriCZZZZ/vim.git ~/.vim
    cd ~/.vim
    ./init.sh
```

## deploy config in repo and install new plugins

``` shell script
    cd ~/.vim
    make sync
```

